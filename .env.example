# ============================================================
# 闲鱼管理平台：源码启动配置模板
#
# 使用方式：首次执行 npm run dev 时会自动复制为 .env.local。
# 只有没有“#”的两项主配置需要填写。
# .env.local 含有真实连接信息和密钥，严禁提交到 Git。
# ============================================================

# 【主配置｜必须填写】
# PostgreSQL 完整连接地址。生产容器统一使用 PostgreSQL；源码模式仍兼容 MySQL。
XIANYU_DATABASE_URL=

# 【主配置｜必须填写】
# Redis 完整连接地址。Redis 密码由你的开发环境提供。
XIANYU_REDIS_URL=

# 【系统自动生成｜不要取消注释】
# 首次启动写入 .env.local；后续启动始终复用，不能随意更换。
# XIANYU_JWT_SECRET=

# 【条件主配置｜通常在管理后台配置】
# Chatwoot 或外部 Webhook 需要固定绝对回调地址时才设置。
# XIANYU_PUBLIC_BASE_URL=https://xianyu.example.com:6161

# 【条件主配置｜仅内部私有 CA】
# Chatwoot 使用私有 HTTPS 证书时填写 CA 文件的绝对路径。
# XIANYU_CHATWOOT_CA_BUNDLE=/absolute/path/to/internal-root.crt

# 【次配置｜同源模式无需修改】
# 前后端分开部署时才配置允许访问 API 的来源。
# XIANYU_CORS_ORIGINS=http://localhost:9001,http://127.0.0.1:9001

# 【次配置｜同源模式无需修改】
# Vite 前端直接访问独立 API 地址时才设置。
# VITE_API_BASE_URL=

# 【条件配置｜仅 Root 开发环境】
# 只有确认当前为隔离开发环境且 Chromium 无法降权运行时才开启。
# XIANYU_IM_VERIFICATION_ALLOW_NO_SANDBOX=true

# 性能、并发、同步周期、浏览器目录等配置均已有安全默认值。
# 需要调优时查阅 docs/configuration.md，不要在首次部署时逐项填写。
