name: xianyu

x-application-environment: &application-environment
  XIANYU_DATABASE_URL: "${XIANYU_DATABASE_URL:?set XIANYU_DATABASE_URL in .env.docker}"
  XIANYU_REDIS_URL: "${XIANYU_REDIS_URL:?set XIANYU_REDIS_URL in .env.docker}"
  XIANYU_JWT_SECRET: "${XIANYU_JWT_SECRET:?set XIANYU_JWT_SECRET in .env.docker}"
  XIANYU_INTERNAL_API_URL: "http://api:8000"
  XIANYU_API_HEALTH_URL: "http://api:8000/api/health"
  XIANYU_PUBLIC_BASE_URL: "${XIANYU_PUBLIC_BASE_URL:-}"
  XIANYU_CORS_ORIGINS: "${XIANYU_CORS_ORIGINS:-}"
  XIANYU_CHATWOOT_CA_BUNDLE: "/etc/xianyu/ca/internal-root.crt"

x-application-volumes: &application-volumes
  - product-images:/data/product-images
  - browser-profiles:/data/browser-profiles
  - fingerprint-chromium:/data/fingerprint-chromium
  - standard-chromium:/data/standard-chromium
  - type: bind
    source: "${XIANYU_INTERNAL_CA_FILE:?set XIANYU_INTERNAL_CA_FILE in .env.docker}"
    target: /etc/xianyu/ca/internal-root.crt
    read_only: true

services:
  mysql:
    image: "${MYSQL_IMAGE:-mysql:8.4}"
    profiles:
      - bundled
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: xianyu_admin
      MYSQL_USER: xianyu
      MYSQL_PASSWORD: "${MYSQL_PASSWORD:-xianyu-change-me}"
      # Keep interpolation valid while this profiled service is disabled.
      # The bundled environment template supplies the required real value.
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD:-}"
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test:
        - CMD-SHELL
        - mysqladmin ping -h 127.0.0.1 -uroot -p"$$MYSQL_ROOT_PASSWORD" --silent
      interval: 10s
      timeout: 5s
      retries: 12

  redis:
    image: "${REDIS_IMAGE:-redis:7.4-alpine}"
    profiles:
      - bundled
    restart: unless-stopped
    environment:
      REDIS_PASSWORD: "${REDIS_PASSWORD:-xianyu-change-me}"
    command:
      - redis-server
      - --appendonly
      - "yes"
      - --requirepass
      - "${REDIS_PASSWORD:-xianyu-change-me}"
    volumes:
      - redis-data:/data
    healthcheck:
      test:
        - CMD-SHELL
        - redis-cli -a "$$REDIS_PASSWORD" ping
      interval: 10s
      timeout: 5s
      retries: 12

  api:
    image: "${XIANYU_IMAGE:-xianyu-admin:local}"
    build:
      context: .
      args:
        BASE_IMAGE_PREFIX: "${BASE_IMAGE_PREFIX:-docker.io/library}"
        BUILD_VERSION: "${XIANYU_VERSION:-development}"
        BUILD_COMMIT: "${XIANYU_COMMIT:-unknown}"
    command:
      - api
    restart: unless-stopped
    environment:
      <<: *application-environment
    volumes: *application-volumes
    shm_size: 1gb
    depends_on:
      mysql:
        condition: service_healthy
        required: false
      redis:
        condition: service_healthy
        required: false
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3).read()"
      interval: 10s
      timeout: 5s
      retries: 12

  worker:
    image: "${XIANYU_IMAGE:-xianyu-admin:local}"
    command:
      - worker
    restart: unless-stopped
    environment:
      <<: *application-environment
    volumes: *application-volumes
    depends_on:
      api:
        condition: service_healthy

  gateway:
    image: "${XIANYU_IMAGE:-xianyu-admin:local}"
    command:
      - gateway
    restart: unless-stopped
    ports:
      - "${XIANYU_BIND_IP:-0.0.0.0}:${XIANYU_HTTPS_PORT:-6161}:8443"
    volumes:
      - type: bind
        source: "${XIANYU_TLS_DIR:?set XIANYU_TLS_DIR in .env.docker}"
        target: /run/secrets/xianyu-tls
        read_only: true
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        - CMD
        - python
        - -c
        - "import ssl,urllib.request; urllib.request.urlopen('https://127.0.0.1:8443/api/health', context=ssl._create_unverified_context(), timeout=3).read()"
      interval: 10s
      timeout: 5s
      retries: 12

volumes:
  mysql-data:
  redis-data:
  product-images:
  browser-profiles:
  fingerprint-chromium:
  standard-chromium:
